name: Hello Worl Workflow

on:
  push:
    branches: [ main ]

jobs:
    hello-job:
        runs-on: ubuntu-latest

        steps:
            - name: Hello World
              run: echo "Hello World!"